services:
  web:
    restart: ${RESTART_CONTAINERS:-unless-stopped}
    image: $DOCKER_REGISTRY/oma:${IMAGE_TAG:-latest}
    build:
      context: ..
      dockerfile: for_docker/oma/Dockerfile
      secrets:
        - labgit
    command: uwsgi --http-socket 0.0.0.0:8000 --master --buffer-size 8192 --processes 3 --module pybrowser_dev.wsgi --vacuum --max-requests=500
    environment:
      - CONTAINER_ROLE=web
      - AUTO_INIT=true
    env_file:
      - env
    volumes:
      # uncomment the following line if you want to use transparently the current checkout mounted
      # into your docker image. convenient for development, but not encouraged in production
      # - ..:/usr/src/pyomabrowser
      - type: volume
        source: static_volume
        target: ${DARWIN_BROWSERSTATIC_PATH}
      - type: volume
        source: media_volume
        target: ${DARWIN_BROWSERMEDIA_PATH}

      # bind mount of release data
      - type: bind
        source: $RELEASE_PATH_HOST
        target: $RELEASE_PATH
        read_only: true

      # alternative if you want to have a docker volume for the release data, comment above and uncomment below
      #- release_volume:${RELEASE_PATH}

    ports:
      - 8000
    depends_on:
      broker:
        condition: service_healthy
      db:
        condition: service_healthy

  db:
    restart: ${RESTART_CONTAINERS:-unless-stopped}
    image: postgres:alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_USER: ${SQL_USER}
      POSTGRES_PASSWORD: ${SQL_PASSWORD}
      POSTGRES_DB: ${SQL_DATABASE}
    shm_size: 256M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${SQL_USER} -d ${SQL_DATABASE}"]
      interval: 5s
      timeout: 5s
      retries: 5

  broker:
    restart: ${RESTART_CONTAINERS:-unless-stopped}
    image: rabbitmq:3
    env_file:
      - env
    ports:
      - 5672:5672
    healthcheck:
        test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
        interval: 5s
        timeout: 5s
        retries: 5

  worker:
    restart: ${RESTART_CONTAINERS:-unless-stopped}
    image: $DOCKER_REGISTRY/oma
    command: ["celery", "--app=pybrowser_dev.celery_app", "worker", "--concurrency=1",
              "--queues=celery,async_web", "--hostname=worker@%h", "--loglevel=INFO"]
    environment:
      - CONTAINER_ROLE=worker
    env_file:
      - env
    volumes_from:
      - web
    depends_on:
      - broker
      - db

  worker_long:
    restart: ${RESTART_CONTAINERS:-unless-stopped}
    image: $DOCKER_REGISTRY/oma
    command: ["celery", "--app=pybrowser_dev.celery_app", "worker", "--concurrency=1",
              "--queues=celery,async_web,long", "--hostname=worker@%h", "--loglevel=INFO"]
    environment:
      - CONTAINER_ROLE=worker
    env_file:
      - env
    volumes_from:
      - web
    volumes:
      - type: bind
        source: ${ALLALL_PATH_HOST}
        target: ${DARWIN_ALLALL_PATH}
        read_only: true

    depends_on:
      - broker
      - db

  nginx:
    restart: ${RESTART_CONTAINERS:-unless-stopped}
    image: $DOCKER_REGISTRY/nginx:${IMAGE_TAG:-latest}
    build:
      context: ./nginx
    ports:
      - 80:80
    volumes:
      - static_volume:${DARWIN_BROWSERSTATIC_PATH}
      - media_volume:${DARWIN_BROWSERMEDIA_PATH}
      # - release_volume:${RELEASE_PATH}
      # use a bind mount if you have the browser data on the host filesystem
      - type: bind
        source: "${RELEASE_PATH_HOST}/downloads"
        target: ${DARWIN_BROWSERDOWNLOAD_PATH}
        read_only: true
    environment:
      - DEPLOYMENT_TYPE=${DEPLOYMENT_TYPE}
      - DARWIN_BROWSERSTATIC_PATH=${DARWIN_BROWSERSTATIC_PATH}
      - DARWIN_BROWSERMEDIA_PATH=${DARWIN_BROWSERMEDIA_PATH}
      - DARWIN_BROWSERDOWNLOAD_PATH=${DARWIN_BROWSERDOWNLOAD_PATH}
    depends_on:
      - web

volumes:
  postgres_data:
  static_volume:
  media_volume:
  # if release data is used with bind mount, comment release_volume line
  #release_volume:


secrets:
  labgit:
    file: "./labgit.cnf"
